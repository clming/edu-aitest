# 数据库配置

## MySQL 连接信息

```bash
# 数据库地址
DATABASE_HOST=mysql-2a840bd18e4b-public.rds.volces.com
DATABASE_PORT=33060

# 数据库凭证
DATABASE_USER=openclaw-edutest
DATABASE_PASSWORD=EZi3fxB9Kpqyap%Dm

# 数据库名称
DATABASE_NAME=edu_assistant

# 完整 DSN (Data Source Name)
DATABASE_URL=openclaw-edutest:EZi3fxB9Kpqyap%Dm@tcp(mysql-2a840bd18e4b-public.rds.volces.com:33060)/edu_assistant?charset=utf8mb4&parseTime=True&loc=Local&interpolateParams=true
```

## Redis 连接信息

```bash
# Redis 地址
REDIS_URL=redis://localhost:6379
```

## JWT 配置

```bash
# JWT 密钥（生产环境必须设置）
JWT_SECRET=your-production-secret-key-here

# 运行环境
ENV=production

# 服务端口
PORT=8080
```

---

## 快速启动

### 1. 复制环境变量文件

```bash
cp .env.example .env
```

### 2. 设置 JWT 密钥

```bash
# 生成随机密钥
openssl rand -hex 32

# 编辑 .env 文件，设置 JWT_SECRET
```

### 3. 启动服务

```bash
# 开发环境
go run cmd/main.go

# 生产环境（Docker）
docker-compose up -d
```

---

## 数据库自动迁移

### 特性

1. **自动创建数据库** - 系统启动时如果数据库不存在会自动创建
2. **自动创建表** - 系统启动时如果表不存在会自动创建
3. **自动添加字段** - 版本迭代时如有新字段会自动添加到现有表
4. **安全迁移** - 不会删除已存在的字段或表

### 添加新表

在 `pkg/models/` 目录下创建新模型，然后在 `internal/database/database.go` 的 `Init()` 函数中添加到 `AutoMigrate`：

```go
if err := DB.AutoMigrate(
    &models.User{},
    &models.Homework{},
    &models.Student{},
    &models.Report{},
    &models.NewModel{},  // 新增模型
); err != nil {
    // ...
}
```

### 添加新字段

在模型结构体中添加新字段：

```go
type User struct {
    ID        uint           `gorm:"primaryKey"`
    Username  string         `gorm:"unique;size:50"`
    Email     string         `gorm:"size:100"`  // 新增字段
    // ...
}
```

系统重启后会自动迁移新字段到数据库。

---

## 数据库连接测试

```bash
# 使用 MySQL 客户端连接
mysql -h mysql-2a840bd18e4b-public.rds.volces.com -P 33060 -u openclaw-edutest -p

# 输入密码：EZi3fxB9Kpqyap%Dm
```

---

## 注意事项

1. **JWT_SECRET** - 生产环境必须设置，且不能泄露
2. **数据库密码** - 包含特殊字符 `%`，在脚本中使用时需要转义
3. **字符集** - 使用 utf8mb4 支持 emoji 等特殊字符
4. **时区** - 使用 `loc=Local` 使用本地时区

---

**更新时间**: 2026-03-20  
**版本**: v1.1
